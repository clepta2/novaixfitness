// src/services/eventTracker.ts
// Re-export shim → analytics/eventTracker.ts

export { trackEvent, Events, EVENT_TYPES, trackScreen, trackWorkoutStart, trackWorkoutComplete, trackError } from './analytics/eventTracker';
