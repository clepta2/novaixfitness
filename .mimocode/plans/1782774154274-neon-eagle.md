# Plano: Dividir Barrel File `src/components/index.js`

## Problema
`src/components/index.js` tem **237 linhas** — excede o limite de 200 do AGENTS.md.

## Objetivo
Reduzir `src/components/index.js` para **~170 linhas** criando sub-barrel files por categoria, mantendo **100% de retrocompatibilidade** com todos os imports existentes.

## Estratégia

Criar arquivos `barrel.js` em cada subdiretório (onde `index.js` já existe com componentes) ou `index.js` (onde não existe). O `index.js` principal re-exporta tudo via `export * from`.

**Importante**: Diretórios `ui/`, `marketplace/` e `onboarding/` já possuem `index.js` com componentes — usaremos `barrel.js` nesses para evitar conflito.

## Arquivos a Criar (16 novos barrel files)

| # | Caminho | Exports | Linhas estimadas |
|---|---------|---------|-----------------|
| 1 | `src/components/workout/barrel.js` | 27 exports | ~29 |
| 2 | `src/components/social/barrel.js` | 12 exports | ~14 |
| 3 | `src/components/profile/barrel.js` | 15 exports (14 + ReferralCard) | ~17 |
| 4 | `src/components/home/barrel.js` | 12 exports | ~14 |
| 5 | `src/components/paywall/barrel.js` | 7 exports | ~9 |
| 6 | `src/components/admin/barrel.js` | 8 exports | ~10 |
| 7 | `src/components/common/barrel.js` | 15 exports | ~17 |
| 8 | `src/components/analytics/barrel.js` | 12 exports | ~14 |
| 9 | `src/components/chat/barrel.js` | 4 exports | ~6 |
| 10 | `src/components/landing/barrel.js` | 7 exports | ~9 |
| 11 | `src/components/progress/barrel.js` | 17 exports | ~19 |
| 12 | `src/components/settings/barrel.js` | 7 exports | ~9 |
| 13 | `src/components/planner/barrel.js` | 8 exports | ~10 |
| 14 | `src/components/faq/barrel.js` | 2 exports (FaqItem, ContactCard) | ~4 |
| 15 | `src/components/auth/barrel.js` | 3 exports | ~5 |
| 16 | `src/components/gamification/barrel.js` | 7 exports (4 Gamification + 2 Notifications + 1 Waitlist) | ~9 |

**Arquivos existentes que não devem ser alterados:**
- `src/components/ui/index.js` (já funciona como barrel)
- `src/components/marketplace/index.js` (já funciona como barrel)
- `src/components/onboarding/index.js` (já funciona como barrel)

## Arquivo Modificado (1 principal)

### `src/components/index.js` — Reescrito (~170 linhas)

Será reescrito com:
1. **16 imports** de sub-barrels (1 linha cada)
2. **~150 re-exports** (1 linha cada, agrupados por seção com comentários)

Nova estrutura:
```javascript
// === UI ===
export * from './ui';

// === WORKOUT ===
export * from './workout/barrel';

// === SOCIAL ===
export * from './social/barrel';

// ... etc para cada grupo

// === GAMIFICATION + NOTIFICATIONS + WAITLIST ===
export * from './gamification/barrel';
```

## Arquivos que NÃO precisam mudar

Todos os **53 arquivos** que importam de `'../src/components'` continuam funcionando sem alteração, pois o barrel principal re-exporta tudo.

Todos os **48 arquivos** com imports diretos (`'../src/components/ui/Button'` etc.) também não são afetados.

## Ordem de Execução

1. Criar os 16 barrel files (sem dependências entre si — podem ser criados em paralelo)
2. Reescrever `src/components/index.js` com os re-exports dos sub-barrels
3. Verificar que nenhum import quebrou (testar com `npx jest --passWithNoTests` ou verificação manual)

## Verificação

1. Rodar `npx jest --passWithNoTests` para garantir que testes passam
2. Verificar que `src/components/index.js` tem < 200 linhas
3. Grep por imports de `'../src/components'` e confirmar que nenhum quebrou
4. Verificar que cada barrel file novo tem < 200 linhas
